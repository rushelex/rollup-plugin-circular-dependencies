{
  "name": "test",
  "type": "module",
  "private": true,
  "engines": {
    "node": ">=18.19"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:update-snapshots": "npm test -- --update",
    "posttest": "rimraf dist"
  },
  "devDependencies": {
    "@rollup/plugin-typescript": "12.1.1",
    "rimraf": "6.0.1",
    "rollup": "4.24.0",
    "rollup-plugin-circular-dependencies": "file:..",
    "rollup-plugin-esbuild": "6.1.1",
    "vitest": "2.1.3"
  }
}
